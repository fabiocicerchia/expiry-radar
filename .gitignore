bin
coverage.out
.DS_Store
reports/
site/
.gandalf-trend.jsonl

# The local config. `expiry-radar.example.json` is the one that belongs in git;
# a real one names internal hostnames, cloud regions and profiles — an estate
# map, in a repository. Anchored, so only the one at the root is ignored.
/expiry-radar.json
